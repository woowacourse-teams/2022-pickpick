import ThemeToggler from ".";

export default {
  title: "@Component/ThemeToggler",
  component: ThemeToggler,
};

const Template = (args) => <ThemeToggler {...args} />;

export const DefaultTemplate = Template.bind({});
