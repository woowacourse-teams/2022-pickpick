import EmptyStatus from ".";

export default {
  title: "Component/EmptyStatus",
  component: EmptyStatus,
};

const Template = (args) => <EmptyStatus {...args} />;

export const DefaultTemplate = Template.bind({});
